name: Teste

on: push

env:
  variavel_global: "valor global"

jobs:
  primeiro:
    runs-on: ubuntu-latest
    steps:
      - name: imprimir
        run: |
          echo global=${{ env.variavel_global }}
  segundo:
    runs-on: ubuntu-latest
    steps:
      - name: imprimir
        run: |
          echo global=${{ env.variavel_global }}
