name: Teste

on: push


jobs:
  primeiro:
    runs-on: ubuntu-latest
    steps:
      - name: imprimir
        run: |
          echo runner=${{ runner.os }}
          echo workflow=${{ github.workflow }}
          echo commit=${{ github.sha }}