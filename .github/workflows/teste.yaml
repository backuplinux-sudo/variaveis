name: Teste

on: 
  workflow_dispatch:
  pull_request:


jobs:
  # usando_curl:
  #   uses: backuplinux-sudo/reutilizaveis/.github/workflows/acesso.yaml@main
  #   with:
  #     site: ${{ inputs.site }}
  #   secrets: inherit
  
  outputs:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo "ref=${{ github.ref }}"
          echo "base_ref=${{ github.base_ref }}"
          echo "head_ref=${{ github.head_ref }}"
          echo "ref_name=${{ github.ref_name }}"

          if [[ "${{ github.head_ref }}" == bug-* ]]; then
            echo "A branch começa com 'bug-'"
          else
            echo "A branch não começa com 'bug-'"
          fi
