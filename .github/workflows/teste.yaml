name: Teste

on: push


jobs:
  primeiro:
    env:
      teste: "funciona"
    runs-on: ubuntu-latest
    steps:
      - name: imprimir
        run: |
          echo usuario=${{ vars.usuario }}
          echo senha="${{ secrets.senha }}" >> teste.txt
          cat teste.txt
